# 验证和验收报告

## 结论

PASS（限问题一、问题二阶段稿范围）。本次已审查 `修改意见/B2.4.txt`，其中关于二次可行域、连续鲁棒目标与离散计算混用、开集极值、5 米边界、目标优先级、角度周期、容差量纲以及问题四推广范围的意见成立，已完成相应修订。未采纳需要额外数据或程序结果才能支撑的结论，也未编造数值算例。

## 检查项

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 二次位置可行域 | 已修正 | 式（37）改为 $P_2=C_1\cap W_2$，明确使用目标圆域、首次示向和最大接收距离等全部已有信息。 |
| 圆弧区域与第一问算法衔接 | 已补全 | 理论模型保留精确圆弧边界；数值实现使用外接正多边形 $\widehat C_{1,N}$，并说明逐情景直径为保守外扩评价。 |
| 连续最坏值 | 已修正 | 式（38）使用 $\sup$，避免对开集 $C_1^{\mathrm{dir}}$ 未经证明地使用最大值。 |
| 理论量与数值量 | 已区分 | 分别定义 $D_{\mathrm{wc}}$、$\widehat D_{\mathrm{sample},N}$、$\widehat D_{*,M,N}$ 和离散近似最优集合 $\mathcal Q_{\Delta,M,N}$。 |
| 5 米边界 | 已修正 | 排除圆边界只作极限情形复核，在 $C_1^{\mathrm{dir}}$ 内从外侧逐步逼近，不再称其为合法样本。 |
| 分层目标顺序 | 已统一 | 接收可靠性由硬约束保证；最终顺序为定位效果优先、移动时间其次，交会角只作解释和搜索引导。 |
| 圆周角误差 | 已修正 | 模型假设和第二次示向度均使用圆周取模，避免 $0^\circ/360^\circ$ 附近的普通绝对差错误。 |
| 几何容差 | 已修正 | 区分距离容差 $\tau_d$、方向容差 $\tau_\theta$ 和叉积容差 $\tau_\times$。 |
| 第一问覆盖判据 | 已修正 | 精确命题改为 $R_{\mathrm{MEC}}(P)=d(P)/2$；带容差的固定圆心检验与最小包围圆检验仅作为独立数值复核。 |
| 接收区域名称 | 已修正 | 改称基于统一下界的保证接收区域和保守可靠候选区域，不再暗示它是所有保证接收点的最大集合。 |
| 数值检验表述 | 已修正 | 网格加密只支持数值稳定性结论，不再宣称证明连续全局最优；当前章节明确为检验方案。 |
| 后续推广 | 已收窄 | 全向源保留全部历史约束；定向源还需发射朝向或独立搜索机制，不能只凭距离保证接收。 |
| 式（27）的径向范围 | 已澄清 | $0\le r\le1500$ 是由 $C_1\subseteq B(S_1,1500)\cap W_1$ 得到的必要范围，目标圆域只会进一步缩小可取区间。 |
| 公式编号 | 通过 | 公式从（1）连续编号到（42），式（17）、式（37）、式（38）均存在且引用正确。 |

## 未直接采纳的建议

- 未强制构造角度覆盖网格上界 $\overline D$。当前稿已如实将有限网格结果表述为离散近似，待程序实现后可根据精度要求决定是否进一步加入严格上界算法。
- 未扩展 $Q_{\mathrm{det}}$ 至利用首次接收半径条件的更大区域。当前区域虽然保守，但具有清晰的确定性接收保证，已避免使用“完整最大区域”的措辞。
- 未补写推荐坐标、基线对比和网格加密数值，因为目前没有具体首次观测输入和运行结果；这些内容必须由后续程序计算产生。

## 章节结构

当前阶段稿包含摘要、目录、问题重述、问题分析、模型假设、符号说明、问题一、问题二和参考文献。问题三、问题四尚未接入，因此本报告不将该阶段稿判定为最终提交版。

## 数值一致性

已核对 $1^\circ$ 测向误差、1800 米目标半径、1000--1500 米有效接收半径、750 米构造点、约 750.23 米最远距离以及 5 米近距离条件。式（27）的扩大参数域证明仍成立，且 $B^\circ(q_0,249)\subseteq Q_{\mathrm{det}}$ 的邻域论证已写入正文。论文未写入未经计算产生的最优点数值。

## 文本质量门禁

本机没有 Bash，未直接运行 Bash 版检查脚本，已使用等价的本地文本检查完成门禁。未发现 TODO、PLACEHOLDER、待补充、未定义引用、重复公式标签或内部工作流路径；入口文件引用的章节均存在。

## 编译

论文使用 XeLaTeX 连续编译两遍，生成 17 页 A4 PDF。日志中不存在 LaTeX Error、未定义引用、Overfull 或 Underfull。辅助文件识别 42 个公式标签，编号从（1）连续至（42）。MiKTeX 仅报告更新检查提示和黑体加粗回退提示，均未影响生成结果。

## PDF 视觉检查

已将新版 PDF 的 17 页全部渲染检查，并以原始分辨率重点核对符号表、第一问式（17）--（19）、第二问式（35）--（42）、有限样本定义、算法步骤、数值检验方案和参考文献。新增公式与表格完整显示，无乱码、重叠、裁切或越界。

## 仍需处理的问题

- 根据具体首次检测输入实现问题二程序，报告 $\widehat D_{\mathrm{sample},N}$、网格层级、近似最优候选集和接收边界复核值。
- 完成问题三、问题四后，重新撰写覆盖全部问题和关键数值的最终摘要。
- 程序完成后补充数值算例、基线对照、灵敏度分析和代码可复现性验收。
